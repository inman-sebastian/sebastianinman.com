{
  "name": "paperwork-app",
  "version": "1.0.0",
  "private": true,
  "description": "Local-only document generator for client paperwork. Runs independently from the website; never deployed.",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^12.0.0"
  }
}
